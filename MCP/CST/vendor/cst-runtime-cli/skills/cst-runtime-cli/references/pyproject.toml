[project]
name = "cst-workspace"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "cst-runtime",
]

[tool.uv.sources]
cst-runtime = { path = ".cst_runtime" }

[project.optional-dependencies]
optimization = ["optuna", "scikit-learn"]
test = ["pytest"]
