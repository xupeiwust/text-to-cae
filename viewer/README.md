# Text to CAE Viewer

This is the browser viewer for the standalone Text-to-CAE workspace.

```powershell
npm.cmd run dev
npm.cmd run build
```

The app defaults to the CAE workspace and talks to local endpoints under `/__cae/*` provided by `vite.config.mjs`.
